html
    head
        title= title
        link(rel="stylesheet", href="/css/styles.css")
    body
        nav
            each navOption in navOptions
                a(href=navOption.path)=navOption.name
        div(class=["container"])
            h1(id="myH1")= title
            p= "Welcome to the Monster Hunter Express/Pug/MongoDB Demo!"
        script(src="/js/scripts.js")